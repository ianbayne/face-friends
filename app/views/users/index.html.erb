<h1>Result of user search:</h1>
<% unless @users.empty? %>
  <ul>
    <% @users.each do |user| %>
      <li>
        <%= link_to "#{user.full_name}", user_path(user) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No users found</p>
<% end %>
